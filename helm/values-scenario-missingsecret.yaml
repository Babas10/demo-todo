# Scenario: Missing Secret (CreateContainerConfigError)
# Deploys without creating the secret. The pod cannot start because
# the secretKeyRef for ADMIN_TOKEN cannot be resolved.
# The agent detects CreateContainerConfigError and creates the missing secret.
secret:
  create: false
